import styled from 'styled-components';

const S = {};

S.ButtonCss = styled.button`
  border-radius: 10px;
  background-color: #53140d;
  border: none;
  cursor: pointer;
`;
S.CardBox = styled.div`
  border-radius: 12px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
`;

export default S;
